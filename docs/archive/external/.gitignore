# External Context Directory - Git Ignore Rules

# Environment variables (NEVER commit)
.env
.env.local
.env.production

# Data files (potentially sensitive)
data/*.csv
data/*.json
data/*.db
data/*.sqlite

# Credentials & Secrets
**/credentials.json
**/secrets.yaml
**/*.key
**/*.pem

# API Keys & Tokens
**/api-keys.txt
**/tokens.json

# Large files (use Git LFS if needed)
data/*.zip
data/*.tar.gz
docs/*.pdf

# Temporary files
*.tmp
*.cache
.DS_Store

# Node modules (if tools have dependencies)
tools/node_modules/
scripts/node_modules/

# Build outputs
tools/dist/
scripts/dist/
